name: build
on: 
  push:
    branches:
      - main
    pull_request:
      branches:
        - main

jobs:
  build:
    runs-on: github-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3
